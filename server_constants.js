/*
 * constants library for rps server (this does *not* get loaded in the browser)
 */

const GAME_ROUNDS = 3; // number of rounds opponents play in each game (default 100)

exports.constants = {'GAME_ROUNDS': GAME_ROUNDS};
